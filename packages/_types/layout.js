export const props = {
  containerStyle: {
    type: [Object, String],
    default: () => ({
      display: "flex",
      width: "100%",
      height: "100%",
      "flex-direction": "column"
    })
  }
}

export const emits = []
